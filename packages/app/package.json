{
  "name": "litscan-demo-app",
  "version": "0.0.1",
  "private": true,
  "description": "Demo app for LitScan",
  "type": "module",
  "scripts": {
    "dev": "cp ../../skill.md public/ && vite",
    "build": "cp ../../skill.md public/ && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
